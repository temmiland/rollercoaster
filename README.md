# Rollercoaster

Reusable libGDX world and rendering library for the Trackside games.

The library is built as `land.temmi:rollercoaster:0.1.0`. During local development, consuming
projects can include this repository with `includeBuild('../rollercoaster')`.

```sh
./gradlew build
```
